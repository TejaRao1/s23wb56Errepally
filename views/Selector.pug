extends layout

block content
  h1= ""
   p Welcome to #{title}
  - var images = ["classroom01.jpg","classroom02.jpg","classroom03.jpg"]
  - for(let i=0; i<3;i++){
    -var num = images[Math.floor(Math.random() * 5)]
    img(src= "/images/"+num, alt= num, style={width: "300px",height: "240px"})
  -}