extends layout

block content
  h1= ""
   p Welcome to #{title}
  - public var images = ["classroom01.jpg","classroom02.jpg","classroom03.jpg","classroom04.jpg"]
  - for(let i=0; i<3;i++){
   -let variable = Math.floor(Math.random()*4)
    img(src=`images/${images[variable]}`,height="300px",width="300px")
  -}